{
  "name": "discord-bot",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/Index.ts",
    "typecheck": "tsc --noEmit",
    "format": "biome format --write .",
    "check": "biome check .",
    "build": "bun run typecheck && bun run check && bun build ./src/Index.ts --target=bun > /dev/null"
  },
  "engines": {
    "bun": ">=1.3"
  },
  "dependencies": {
    "discord.js": "^14.26.4",
    "ipaddr.js": "^2.4.0",
    "jimp": "^1.6.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.4",
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
